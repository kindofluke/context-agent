'''
Create a class that can be JS execution environment 

A static file "tools.js" should be created

 that has file system functions 
 `cat`: to read files
`find`: to find files 
`grep`: to find text 
`tree`: to understand the files available for context. 
`write`: to create a new file or overwrite a new file
`search_and_replace`: to change the text of a new file

 and returns the results as a string.

 this module should also have the execute JS function in python that will be added to the agent


'''